# Арбитражный бот для Solana

Надёжный арбитражный бот на Rust для поиска и исполнения прибыльных сделок между различными DEX на блокчейне Solana.

## Особенности

- 🔒 **Безопасность**: Режим симуляции по умолчанию, защита ключей
- ⚡ **Производительность**: Асинхронная архитектура на Tokio
- 🎯 **Точность**: Использование rust_decimal для финансовых расчётов
- 📊 **Мониторинг**: Детальное логирование всех операций
- 🔧 **Гибкость**: Модульная архитектура, легко добавлять новые DEX

## Требования

- Rust 1.75.0 или новее
- Ubuntu 22.04+ (или другая Linux-система)
- Доступ к Solana RPC endpoint

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd arb-bot
```

2. Запустите скрипт установки:
```bash
chmod +x install.sh
./install.sh
```

3. Настройте конфигурацию:
```bash
cp config.example.toml config.toml
chmod 600 config.toml
# Отредактируйте config.toml под свои нужды
```

4. Настройте секреты (опционально):
```bash
cp .env.example .env
chmod 600 .env
# Заполните .env реальными значениями
```

5. Соберите проект:
```bash
cargo build --release
```

## Использование

### Запуск в режиме симуляции

```bash
./start.sh
```

### Установка как systemd сервис

```bash
sudo cp arb-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable arb-bot.service
sudo systemctl start arb-bot.service
```

### Остановка

```bash
./stop.sh
# или
sudo systemctl stop arb-bot.service
```

### Резервное копирование ключей

```bash
./backup_keys.sh
```

## Конфигурация

Основные параметры настраиваются в `config.toml`:

- **network**: RPC endpoint и настройки сети
- **wallet**: Путь к файлу ключа
- **arbitrage**: Параметры торговли (минимальная прибыль, объём, slippage)
- **dex**: Список активированных DEX и торговых пар
- **monitoring**: Интервал проверки и уровень логирования
- **safety**: Режим симуляции и защитные механизмы

⚠️ **ВАЖНО**: Всегда тестируйте в режиме симуляции (`simulation_mode = true`) перед запуском в продакшн!

## Структура проекта

```
arb-bot/
├── src/
│   ├── main.rs          # Точка входа
│   ├── config.rs        # Загрузка конфигурации
│   ├── wallet.rs        # Управление кошельком
│   ├── dex.rs           # Интерфейс DEX
│   ├── arbitrage.rs     # Движок арбитража
│   └── monitor.rs       # Мониторинг и логирование
├── tests/
│   └── integration_test.rs  # Интеграционные тесты
├── config.example.toml   # Шаблон конфигурации
├── install.sh           # Скрипт установки
├── start.sh             # Запуск бота
├── stop.sh              # Остановка бота
├── update.sh            # Обновление бота
├── backup_keys.sh       # Резервное копирование ключей
├── arb-bot.service      # Systemd unit
└── arb-bot.logrotate    # Конфигурация ротации логов
```

## Разработка

### Запуск тестов

```bash
cargo test
```

### Запуск в режиме разработки

```bash
RUST_LOG=debug cargo run
```

## Безопасность

- Никогда не коммитьте `config.toml`, `.env` или файлы ключей
- Используйте права доступа 600 для конфигурационных файлов
- Используйте права доступа 400 для файлов ключей
- Всегда тестируйте в режиме симуляции перед продакшн
- Регулярно делайте резервные копии ключей

## Roadmap

См. [docs/roadmap.md](docs/roadmap.md) для подробного плана развития.

## Лицензия

MIT

