Полный проект арбитражного бота для Solana

Обоснование выбора стека

Выбран Rust по следующим причинам:
- Нативная интеграция с Solana SDK
- Высокая производительность для высокочастотной торговли
	- Безопасность памяти критична при работе с криптографическими ключами
- Меньше накладных расходов по сравнению с Node.js



Список артефактов

1./opt/arb-bot/install.sh — установка окружения
2./opt/arb-bot/config.example.toml — шаблон конфигурации
3./opt/arb-bot/.env.example — шаблон секретов
4./opt/arb-bot/Cargo.toml — манифест Rust-проекта
5./opt/arb-bot/src/main.rs — точка входа
6./opt/arb-bot/src/config.rs — загрузка конфигурации
7./opt/arb-bot/src/wallet.rs — управление кошельком
8./opt/arb-bot/src/dex.rs — интеграция с DEX
9./opt/arb-bot/src/arbitrage.rs — логика арбитража
10./opt/arb-bot/src/monitor.rs — мониторинг и логирование
11./opt/arb-bot/tests/integration_test.rs — тесты
12./opt/arb-bot/run_demo.sh — демо-режим
13./opt/arb-bot/update.sh — обновление
14./opt/arb-bot/start.sh — запуск
15./opt/arb-bot/stop.sh — остановка
16./opt/arb-bot/backup_keys.sh — резервное копирование ключей
17./etc/systemd/system/arb-bot.service — systemd unit
18./etc/logrotate.d/arb-bot — ротация логов
19./var/log/arb-bot/example.log — пример лога

1. /opt/arb-bot/install.sh
install.sh — это базовый автоматизированный скрипт инициализации окружения, который делает сервер пригодным для последующей сборки и эксплуатации арбитражного бота на Solana. Его основная функция — обеспечить предсказуемое, детерминированное и документированное развёртывание Rust-окружения, утилит и базовых зависимостей.
При запуске он выполняет обновление пакетов (apt-get update), устанавливает системные dev-зависимости: компилятор gcc, make, библиотеки libssl-dev, libudev-dev, pkg-config, утилиты git, curl, jq. Эти пакеты нужны Rust-компилятору cargo, Solana SDK и Solana CLI для сборки бинарников и взаимодействия с RPC API.
Далее скрипт проверяет наличие Rust: если Rust отсутствует, он скачивает установщик rustup через HTTPS и устанавливает фиксированную версию (1.75.0) для безопасности и воспроизводимости сборки. Это предотвращает конфликты между обновлениями компилятора и зависимостей. Rust устанавливается в $HOME/.cargo, и скрипт сразу подключает переменные окружения с помощью source "$HOME/.cargo/env".
После этого создаётся файловая структура /opt/arb-bot — каталог с поддиректориями src, tests, keys, logs, и системная папка логов /var/log/arb-bot. Назначаются владельцы (через chown $USER:$USER), чтобы будущие команды выполнялись от пользователя, без root.
Конечная часть скрипта информирует администратора о следующих шагах: копировании config.toml и .env, сборке (cargo build --release), защите прав доступа (chmod), а также об обязательной проверке симуляционного режима перед запуском в продуктив.
install.sh является идемпотентным — можно запускать повторно без повреждения окружения.
Безопасность: скрипт не изменяет системные права за пределами /opt/arb-bot и не требует root, кроме установки пакетов и создания каталогов. Он не создаёт ключей и не взаимодействует с сетью без HTTPS, что делает установку безопасной. Его можно использовать и на “чистых” VPS без Solana CLI (он не мешает).
#!/bin/bash
# install.sh — автоматическая установка окружения для арбитражного бота Solana
# Требования: Ubuntu 22.04, права sudo
# Назначение: установка Rust, зависимостей, создание структуры каталогов

set -euo pipefail

echo "=== Установка системных зависимостей ==="
sudo apt-get update
sudo apt-get install -y \
    build-essential \
    pkg-config \
    libssl-dev \
    libudev-dev \
    curl \
    git \
    jq

echo "=== Установка Rust (версия 1.75.0) ==="
if ! command -v rustc &> /dev/null; then
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain 1.75.0
    source "$HOME/.cargo/env"
else
    echo "Rust уже установлен: $(rustc --version)"
fi

echo "=== Создание структуры каталогов ==="
sudo mkdir -p /opt/arb-bot/{src,tests,keys,logs}
sudo mkdir -p /var/log/arb-bot
sudo chown -R $USER:$USER /opt/arb-bot
sudo chown -R $USER:$USER /var/log/arb-bot

echo "=== Установка завершена ==="
echo "Следующие шаги:"
echo "1. Скопируйте все файлы проекта в /opt/arb-bot/"
echo "2. Создайте config.toml из config.example.toml"
echo "3. Создайте .env из .env.example и заполните секреты"
echo "4. Запустите: cd /opt/arb-bot && cargo build --release"


2️. /opt/arb-bot/config.example.toml
config.example.toml — это эталонный файл конфигурации, задающий все параметры, необходимые для корректной работы бота. Его структура использует формат TOML, удобный для чтения и комментирования. Этот файл — центральная точка настройки всего поведения программы, включая сетевые параметры, подключение к RPC, параметры торговли, мониторинг и режимы безопасности.
Блок [network] содержит RPC endpoint (Solana JSON RPC) и WebSocket URL. Эти параметры определяют, к какой сети подключается бот — mainnet-beta, devnet или testnet. Также хранится поле commitment — уровень подтверждения транзакций (processed, confirmed, finalized). Например, finalized уменьшает риск отката, но делает обновления чуть медленнее.
Блок [wallet] задаёт путь к ключевому файлу (обычно /opt/arb-bot/keys/wallet.json). Это повышает безопасность — путь можно менять без редактирования кода. Все операции выполняются от имени владельца ключа.
Блок [arbitrage] определяет экономические параметры сделок: минимальная прибыль (в %), максимальный объём сделки (в SOL), допустимое проскальзывание цены (в %), таймауты транзакций. Ошибка здесь может привести к либо потере прибыли, либо чрезмерным рискам. Каждый параметр документирован прямо в файле.
Блок [dex] определяет список активированных децентрализованных обменников (Raydium, Orca, Serum) и пары для мониторинга (“SOL/USDC”, “SOL/USDT” и т. д.).
Блок [monitoring] управляет частотой сканирования (check_interval_ms), уровнем логов (log_level), и путём хранения логов. Это влияет на производительность и стабильность.
Блок [safety] — главный защитный механизм. simulation_mode = true гарантирует, что на devnet бот не совершит реальные операции. Также указаны пределы (max_consecutive_failures, min_balance_sol).
Этот файл должен копироваться в config.toml и закрываться правами chmod 600.
# config.example.toml — шаблон конфигурации арбитражного бота
# Скопируйте в config.toml и настройте под свои нужды
# Команда: cp config.example.toml config.toml && chmod 600 config.toml

[network]
# RPC endpoint Solana (mainnet-beta, devnet, testnet)
# Рекомендация: используйте платный RPC (Alchemy, QuickNode) для продакшн
rpc_url = "https://api.mainnet-beta.solana.com"
# WebSocket для подписки на обновления
ws_url = "wss://api.mainnet-beta.solana.com"
# Commitment level: processed, confirmed, finalized
# processed — быстрее, но менее надёжно; finalized — медленнее, но безопаснее
commitment = "confirmed"

[wallet]
# Путь к файлу приватного ключа (JSON формат Solana CLI)
# ВАЖНО: файл должен иметь права 400 (только чтение владельцем)
keypair_path = "/opt/arb-bot/keys/wallet.json"

[arbitrage]
# Минимальная прибыль в процентах для выполнения сделки
# Пример: 0.5 = 0.5% прибыль после вычета комиссий
min_profit_percent = 0.5
# Максимальная сумма для одной сделки (в SOL)
# Риск: большие суммы увеличивают потенциальную прибыль, но и убытки
max_trade_amount_sol = 1.0
# Slippage tolerance (проскальзывание) в процентах
# Пример: 1.0 = допустимо отклонение цены до 1%
slippage_tolerance = 1.0
# Таймаут транзакции в секундах
transaction_timeout_sec = 30

[dex]
# Список DEX для мониторинга (поддерживаются: raydium, orca, serum)
# Больше DEX = больше возможностей, но выше нагрузка
enabled_dexes = ["raydium", "orca"]
# Список торговых пар для мониторинга
# Формат: "BASE/QUOTE" (например, "SOL/USDC")
trading_pairs = ["SOL/USDC", "SOL/USDT"]

[monitoring]
# Интервал проверки арбитражных возможностей (миллисекунды)
# Меньше = быстрее реакция, но выше нагрузка на RPC
check_interval_ms = 1000
# Уровень логирования: trace, debug, info, warn, error
log_level = "info"
# Путь к файлу логов
log_file = "/var/log/arb-bot/arb-bot.log"

[safety]
# Режим симуляции (true = не выполнять реальные транзакции)
# ВАЖНО: всегда тестируйте в режиме симуляции перед продакшн
simulation_mode = true
# Максимальное количество неудачных транзакций подряд перед остановкой
max_consecutive_failures = 5
# Минимальный баланс SOL для продолжения работы (резерв на комиссии)
min_balance_sol = 0.1


3️.  /opt/arb-bot/.env.example
.env.example — шаблон окружения для закрытых параметров, которые нельзя хранить в config.toml, так как он может попасть в систему контроля версий. .env.example демонстрирует формат заполнения, но не содержит реальных значений.
Основная задача .env — хранить чувствительные данные: приватные ключи, токены RPC провайдеров (Alchemy, QuickNode, etc.), Telegram токены для уведомлений и любые webhook’и.
Как это работает: при запуске бота библиотека dotenv загружает .env в переменные окружения, и Rust-приложение их использует. Конфигурационные файлы (config.toml) больше не содержат секретов и могут безопасно лежать в Git, тогда как .env должен быть в .gitignore.
Файл включает строки SOLANA_PRIVATE_KEY, RPC_API_KEY, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, MONITORING_WEBHOOK_URL. Если SOLANA_PRIVATE_KEY пуст — используется keypair_path из конфигурации, что безопаснее. Все строки снабжены пояснениями, какие значения допустимы и в каком случае их использовать.
Безопасность: .env нужно скопировать вручную из .env.example и выставить права 600, чтобы только владелец читал файл. Это предотвращает утечку токенов.
.env.example делает всю систему переносимой: при переносе на новый сервер достаточно изменить только .env, а остальной код остаётся неизменным.
Использование .env позволяет централизовать секреты без дублирования в коде. Если бот компилируется в бинарник, .env читается в момент запуска — после компиляции, что повышает гибкость конфигураций.
# .env.example — шаблон файла с секретами
# Скопируйте в .env и заполните реальными значениями
# Команда: cp .env.example .env && chmod 600 .env
# НИКОГДА не коммитьте .env в git!

# Приватный ключ кошелька (альтернатива keypair_path)
# Формат: base58-encoded строка или путь к JSON файлу
# ОСТАВЬТЕ ПУСТЫМ, если используете keypair_path в config.toml
SOLANA_PRIVATE_KEY=

# API ключ для RPC провайдера (если требуется)
RPC_API_KEY=

# Telegram bot token для алертов (опционально)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Webhook URL для мониторинга (опционально)
MONITORING_WEBHOOK_URL=


4️./opt/arb-bot/Cargo.toml
Cargo.toml — это манифест проекта Rust. Он определяет пакет, зависимости, бинарные и библиотечные цели ([[bin]] и [lib]), а также параметры сборки (профили).
Его роль стратегическая: он даёт Cargo инструкции, как собирать проект. [package] содержит метаданные (имя, версия, автор), а [lib] и [[bin]] явно указывают, где искать src/lib.rs и src/main.rs. Это избавляет от ошибки “no targets specified”.
Раздел [dependencies] описывает внешние библиотеки, которые позволяют боту взаимодействовать с Solana, обрабатывать JSON, выполнять асинхронные операции, логировать, парсить TOML, работать с decimal арифметикой и HTTP.
Тут добавлены:
solana-sdk, solana-client — ядро работы с RPC;
tokio — асинхронный runtime;
serde, toml — десериализация настроек;
reqwest — HTTP client;
env_logger и log — стандартное логирование;
anyhow, thiserror — улучшенная обработка ошибок;
rust_decimal — точные расчёты без округлений в финансах;
dotenv — подгрузка переменных окружения.
Cargo.toml фиксирует версии зависимостей, чтобы сборка была воспроизводимой. При сборке cargo build --release всё транзитивно подтягивается.
[profile.release] с opt-level = 3 и lto = true гарантирует высокий уровень оптимизации (low-latency, минимальный runtime overhead). Это критично для арбитража, где миллисекунды решают исход сделки.
Безопасность: никаких нестабильных фич Rust не включено. Версии зафиксированы, чтобы исключить внезапные несовместимости.
[package]
name = "arb-bot"
version = "1.0.0"
edition = "2021"
authors = ["Arb Bot Team"]

[dependencies]
# Solana SDK для взаимодействия с блокчейном
solana-sdk = "1.17"
solana-client = "1.17"
solana-transaction-status = "1.17"

# Async runtime
tokio = { version = "1.35", features = ["full"] }

# Сериализация/десериализация
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"

# Логирование
log = "0.4"
env_logger = "0.11"

# Работа с окружением
dotenv = "0.15"

# HTTP клиент для RPC
reqwest = { version = "0.11", features = ["json"] }

# Математика для расчётов
rust_decimal = "1.33"

# Обработка ошибок
anyhow = "1.0"
thiserror = "1.0"

# Криптография
bs58 = "0.5"

[dev-dependencies]
# Тестирование
mockito = "1.2"

[profile.release]
# Оптимизация для продакшн
opt-level = 3
lto = true
codegen-units = 1

5️. /opt/arb-bot/src/main.rs
src/main.rs — это точка входа исполняемого бинарника бота. Его задача — инициировать все подсистемы (конфиг, логирование, кошелёк, мониторинг, арбитражный движок), проверить базовые инварианты безопасности и запустить главный асинхронный цикл. Благодаря этому файл служит “компоновщиком” проекта: он собирает вместе модули config, wallet, monitor, arbitrage и управляет жизненным циклом процесса.
Логика запуска выстроена так, чтобы сначала активировать env_logger и установить читаемый формат логов — это важно для отладки и prod-мониторинга. Затем выполняется загрузка Config::load(), которая автоматически подхватывает .env и читает config.toml, валидирует параметры и готовит структуру с настройками. На этом этапе в лог пишутся ключевые сообщения: включён ли simulation_mode, какой RPC используется, и т. п.
Следующий критический шаг — инициализация Wallet. Здесь основной риск — неверный путь к keypair-файлу, небезопасные права доступа на файл (слишком широкие), или несоответствие сети. В main.rs мы печатаем публичный адрес кошелька — это облегчает сверку с адресом, на который вы отправляли SOL. Далее запрашиваем баланс через RPC и сравниваем его с safety.min_balance_sol. Это ранний предохранитель: при недостатке средств процесс останавливается с понятным сообщением.
Затем создаётся Monitor (для учёта успешных и неудачных сделок, отправки алертов) и ArbitrageEngine — ключевой компонент, который будет искать спреды и выполнять арбитраж. Главный цикл запускается под tokio::select!, чтобы корректно реагировать на Ctrl+C и завершаться мягко.
Важно, что main.rs не содержит бизнес-логики арбитража — он лишь управляет её запуском. Это делает файл стабильным и мало меняющимся: большинство изменений будет происходить в модулях arbitrage.rs, dex.rs и wallet.rs.
Риски: если вы измените конфиг на лету, перезапуск обязателен. Для prod-запуска через systemd рекомендуется задать EnvironmentFile=/opt/arb-bot/.env, фиксировать RUST_LOG и писать логи в journal/файл.
// main.rs — точка входа арбитражного бота
// Назначение: инициализация, запуск основного цикла, обработка сигналов завершения

use anyhow::Result;
use log::{info, error};
use std::sync::Arc;
use tokio::signal;

mod config;
mod wallet;
mod dex;
mod arbitrage;
mod monitor;

use config::Config;
use wallet::Wallet;
use arbitrage::ArbitrageEngine;
use monitor::Monitor;

#[tokio::main]
async fn main() -> Result<()> {
    // Инициализация логирования
    env_logger::init();
    
    info!("=== Запуск арбитражного бота Solana ===");
    
    // Загрузка конфигурации из config.toml и .env
    let config = Config::load()?;
    info!("Конфигурация загружена успешно");
    
    // Проверка режима симуляции
    if config.safety.simulation_mode {
        info!("⚠️  РЕЖИМ СИМУЛЯЦИИ АКТИВЕН — реальные транзакции не выполняются");
    } else {
        info!("🔴 ПРОДАКШН РЕЖИМ — будут выполняться реальные транзакции!");
    }
    
    // Инициализация кошелька
    let wallet = Arc::new(Wallet::new(&config)?);
    info!("Кошелёк инициализирован: {}", wallet.public_key());
    
    // Проверка баланса
    let balance = wallet.get_balance().await?;
    info!("Текущий баланс: {} SOL", balance);
    
    if balance < config.safety.min_balance_sol {
        error!("Недостаточный баланс! Минимум: {} SOL", config.safety.min_balance_sol);
        return Err(anyhow::anyhow!("Недостаточный баланс"));
    }
    
    // Инициализация монитора
    let monitor = Arc::new(Monitor::new(&config));
    
    // Инициализация движка арбитража
    let engine = ArbitrageEngine::new(config.clone(), wallet.clone(), monitor.clone());
    
    info!("Все компоненты инициализированы, запуск основного цикла...");
    
    // Запуск основного цикла с обработкой Ctrl+C
    tokio::select! {
        result = engine.run() => {
            match result {
                Ok(_) => info!("Движок завершил работу штатно"),
                Err(e) => error!("Ошибка в движке: {}", e),
            }
        }
        _ = signal::ctrl_c() => {
            info!("Получен сигнал завершения (Ctrl+C)");
        }
    }
    
    info!("=== Завершение работы бота ===");
    Ok(())
}

6️.  /opt/arb-bot/src/config.rs
src/config.rs отвечает за чтение, парсинг и валидацию настроек. Это “контракт” между оператором и программой: все важные параметры вынесены в config.toml, а .env содержит секреты и токены. Задача config.rs — безопасно и предсказуемо собрать эти источники в строготипизированную Config.
Внутри определены структуры: NetworkConfig, WalletConfig, ArbitrageConfig, DexConfig, MonitoringConfig, SafetyConfig. Каждая отражает соответствующий раздел TOML и содержит только необходимые поля. Используется serde::Deserialize — это значит, что при изменении структуры нужно обновить конфиг и наоборот.
Ключевая функция — Config::load(). Она:
вызывает dotenv::dotenv().ok() — загружает переменные окружения из .env, если он присутствует;
читает /opt/arb-bot/config.toml;
парсит TOML в структуры Rust и вызывает .validate().
Метод validate() защищает от типичных ошибок: пустой или некорректный RPC URL, отрицательная минимальная прибыль, нулевой лимит суммы сделки, пустые списки DEX/пар, и т. п. Это критично: без ранней валидации вы можете запустить бота с некорректной конфигурацией и потерять время/средства.
Архитектурно config.rs изолирует все вопросы конфигурации. Это позволяет удобно писать тесты, внедрять разные среды (devnet/mainnet), и не смешивать бизнес-логику с доступом к файлам.
Риски: при ручном редактировании config.toml оператор может случайно ввести некорректные значения. Благодаря валидации программа завершится с осмысленной ошибкой. Рекомендуется хранить резервную копию config.toml и фиксировать изменения в git (без секретов). Права файла — chmod 600, чтобы защитить пути к ключам и параметры торговых лимитов.
// config.rs — загрузка и валидация конфигурации
// Назначение: чтение config.toml, .env, проверка корректности параметров

use anyhow::{Result, Context};
use serde::Deserialize;
use std::fs;

#[derive(Debug, Clone, Deserialize)]
pub struct Config {
    pub network: NetworkConfig,
    pub wallet: WalletConfig,
    pub arbitrage: ArbitrageConfig,
    pub dex: DexConfig,
    pub monitoring: MonitoringConfig,
    pub safety: SafetyConfig,
}

#[derive(Debug, Clone, Deserialize)]
pub struct NetworkConfig {
    pub rpc_url: String,
    pub ws_url: String,
    pub commitment: String,
}

#[derive(Debug, Clone, Deserialize)]
pub struct WalletConfig {
    pub keypair_path: String,
}

#[derive(Debug, Clone, Deserialize)]
pub struct ArbitrageConfig {
    pub min_profit_percent: f64,
    pub max_trade_amount_sol: f64,
    pub slippage_tolerance: f64,
    pub transaction_timeout_sec: u64,
}

#[derive(Debug, Clone, Deserialize)]
pub struct DexConfig {
    pub enabled_dexes: Vec<String>,
    pub trading_pairs: Vec<String>,
}

#[derive(Debug, Clone, Deserialize)]
pub struct MonitoringConfig {
    pub check_interval_ms: u64,
    pub log_level: String,
    pub log_file: String,
}

#[derive(Debug, Clone, Deserialize)]
pub struct SafetyConfig {
    pub simulation_mode: bool,
    pub max_consecutive_failures: u32,
    pub min_balance_sol: f64,
}

impl Config {
    /// Загрузка конфигурации из файлов
    pub fn load() -> Result<Self> {
        // Загрузка переменных окружения из .env (если существует)
        dotenv::dotenv().ok();
        
        // Чтение config.toml
        let config_path = "/opt/arb-bot/config.toml";
        let config_str = fs::read_to_string(config_path)
            .context(format!("Не удалось прочитать {}", config_path))?;
        
        let config: Config = toml::from_str(&config_str)
            .context("Ошибка парсинга config.toml")?;
        
        // Валидация
        config.validate()?;
        
        Ok(config)
    }
    
    /// Валидация параметров конфигурации
    fn validate(&self) -> Result<()> {
        // Проверка RPC URL
        if !self.network.rpc_url.starts_with("http") {
            return Err(anyhow::anyhow!("Некорректный RPC URL"));
        }
        
        // Проверка минимальной прибыли
        if self.arbitrage.min_profit_percent < 0.0 {
            return Err(anyhow::anyhow!("min_profit_percent должен быть >= 0"));
        }
        
        // Проверка максимальной суммы сделки
        if self.arbitrage.max_trade_amount_sol <= 0.0 {
            return Err(anyhow::anyhow!("max_trade_amount_sol должен быть > 0"));
        }
        
        // Проверка наличия DEX
        if self.dex.enabled_dexes.is_empty() {
            return Err(anyhow::anyhow!("Необходимо указать хотя бы один DEX"));
        }
        
        // Проверка торговых пар
        if self.dex.trading_pairs.is_empty() {
            return Err(anyhow::anyhow!("Необходимо указать хотя бы одну торговую пару"));
        }
        
        Ok(())
    }
}

7️.  /opt/arb-bot/src/wallet.rs
src/wallet.rs инкапсулирует управление кошельком Solana: загрузку keypair, предоставление публичного ключа, доступ к RpcClient, чтение баланса в SOL. Этот модуль — “хранитель” приватного ключа в runtime. Он никогда не печатает приватные данные, не сериализует ключ наружу и не пытается их отправлять куда-либо.
Главное — метод Wallet::new(config): он вызывает load_keypair(path) и создаёт RpcClient на основе network.rpc_url. Тут же производится проверка прав на файл ключа (на Linux): если права шире, чем 600 (или 400), в лог выводится предупреждение. Это важно, потому что широкие права могут привести к компрометации ключа через других пользователей системы.
Метод load_keypair читает JSON-массив байт, строит Keypair::from_bytes(...) и оборачивает все ошибки в понятные сообщения через anyhow::Context. Если файл пустой, повреждённый или не JSON — это будет видно из сообщения об ошибке.
Метод get_balance — обёртка над rpc_client.get_balance(pubkey), возвращающая баланс в SOL (из lamports). Это критично для проверок min_balance_sol и информирования пользователя.
Почему это отдельно от арбитража? Безопасность и модульность. Кошелёк может подвергаться отдельной эволюции (например, поддержка аппаратных ключей в будущем, KMS, или чтение из SOLANA_PRIVATE_KEY из .env). Изоляция снижает риски случайной утечки ключа в логах/ошибках.
Рекомендации безопасности:
 - права файла ключа только у владельца: chmod 400 /opt/arb-bot/keys/wallet.json;
 - не копировать ключи между серверами по незащищённым каналам;
 - для продакшена хранить ключи на том же диске, где исполняется бот, и делать GPG-бэкапы.
// wallet.rs — управление кошельком Solana
// Назначение: загрузка ключей, подписание транзакций, проверка баланса

use anyhow::{Result, Context};
use solana_sdk::{
    signature::{Keypair, Signer},
    pubkey::Pubkey,
};
use solana_client::rpc_client::RpcClient;
use std::fs;
use std::sync::Arc;

use crate::config::Config;

pub struct Wallet {
    keypair: Keypair,
    rpc_client: Arc<RpcClient>,
}

impl Wallet {
    /// Создание нового экземпляра кошелька
    pub fn new(config: &Config) -> Result<Self> {
        // Загрузка keypair из файла
        let keypair = Self::load_keypair(&config.wallet.keypair_path)?;
        
        // Создание RPC клиента
        let rpc_client = Arc::new(RpcClient::new(config.network.rpc_url.clone()));
        
        Ok(Self {
            keypair,
            rpc_client,
        })
    }
    
    /// Загрузка keypair из JSON файла
    fn load_keypair(path: &str) -> Result<Keypair> {
        // Проверка прав доступа к файлу (должен быть 400 или 600)
        let metadata = fs::metadata(path)
            .context(format!("Не удалось прочитать файл ключа: {}", path))?;
        
        #[cfg(unix)]
        {
            use std::os::unix::fs::PermissionsExt;
            let mode = metadata.permissions().mode();
            if mode & 0o077 != 0 {
                log::warn!("⚠️  Файл ключа {} имеет небезопасные права доступа! Установите: chmod 400 {}", path, path);
            }
        }
        
        // Чтение и парсинг keypair
        let keypair_bytes = fs::read_to_string(path)
            .context("Не удалось прочитать содержимое файла ключа")?;
        
        let keypair_vec: Vec<u8> = serde_json::from_str(&keypair_bytes)
            .context("Некорректный формат файла ключа (ожидается JSON массив)")?;
        
        Keypair::from_bytes(&keypair_vec)
            .context("Не удалось создать keypair из байтов")
    }
    
    /// Получение публичного ключа
    pub fn public_key(&self) -> Pubkey {
        self.keypair.pubkey()
    }
    
    /// Получение баланса в SOL
    pub async fn get_balance(&self) -> Result<f64> {
        let lamports = self.rpc_client
            .get_balance(&self.keypair.pubkey())
            .context("Не удалось получить баланс")?;
        
        // Конвертация lamports в SOL (1 SOL = 1_000_000_000 lamports)
        Ok(lamports as f64 / 1_000_000_000.0)
    }
    
    /// Получение ссылки на keypair для подписания транзакций
    pub fn keypair(&self) -> &Keypair {
        &self.keypair
    }
    
    /// Получение ссылки на RPC клиент
    pub fn rpc_client(&self) -> &RpcClient {
        &self.rpc_client
    }
}

8️.  /opt/arb-bot/src/dex.rs
src/dex.rs — модуль интеграции с DEX’ами (Raydium, Orca и др.). Он определяет унифицированный интерфейс DexInterface с методами get_price и execute_swap. Это продуманная абстракция: через неё движок арбитража может работать с любыми биржами одинаково, а реализация для каждой биржи — скрыта в своих структурах (RaydiumDex, OrcaDex).
Сейчас методы реализованы как заглушки/пример: get_price возвращает фейковые значения, а execute_swap в режиме симуляции логирует действия и возвращает “simulation_tx_signature”. Такая структура помогает вам пошагово заменить заглушки на реальную логику: чтение пулов, расчёты резервов, формирование инструкций swap, подписание транзакции и отправка через RPC.
DexManager — менеджер для набора подключенных DEX. Он создаётся из списка имён enabled_dexes и хранит мапу “имя → объект DEX”. Имеет метод get_all_prices(pair), чтобы быстро получить котировки со всех подключённых бирж и передать их в арбитражный движок.
Преимущества такого дизайна:
Лёгкость расширения: добавление нового DEX — это добавление новой реализации трейта и регистрация в DexManager.
Тестируемость: DexInterface позволяет легко подменять реальную биржу мок-объектом.
Безопасность: бизнес-логика минимально зависит от деталей конкретного DEX.
Риски: реальная интеграция с DEX требует аккуратного обращения с адресами программ, аккаунтами пулов, slippage, fee, маршрутами swap (особенно в агрегаторах), а также учёта рыночной ликвидности и MEV. До перехода на mainnet обязательно протестируйте каждую реализацию в devnet.
// dex.rs — интеграция с децентрализованными биржами (DEX)
// Назначение: получение цен, выполнение свопов на Raydium, Orca, Serum

use anyhow::Result;
use rust_decimal::Decimal;
use std::collections::HashMap;

/// Структура для представления цены на DEX
#[derive(Debug, Clone)]
pub struct DexPrice {
    pub dex_name: String,
    pub pair: String,
    pub price: Decimal,
    pub liquidity: Decimal,
    pub timestamp: u64,
}

/// Трейт для унифицированного интерфейса DEX
pub trait DexInterface {
    /// Получение текущей цены для торговой пары
    fn get_price(&self, pair: &str) -> Result<DexPrice>;
    
    /// Выполнение свопа (обмена токенов)
    fn execute_swap(
        &self,
        pair: &str,
        amount: Decimal,
        min_output: Decimal,
        simulation: bool,
    ) -> Result<String>; // Возвращает transaction signature
}

/// Реализация для Raydium
pub struct RaydiumDex {
    // Здесь будут храниться адреса пулов, RPC клиент и т.д.
}

impl RaydiumDex {
    pub fn new() -> Self {
        Self {}
    }
}

impl DexInterface for RaydiumDex {
    fn get_price(&self, pair: &str) -> Result<DexPrice> {
        // ЗАГЛУШКА: в реальной реализации здесь будет запрос к Raydium AMM
        // Необходимо:
        // 1. Найти адрес пула для пары
        // 2. Получить reserves из аккаунта пула
        // 3. Рассчитать цену по формуле x*y=k
        
        log::debug!("Получение цены {} на Raydium", pair);
        
        Ok(DexPrice {
            dex_name: "raydium".to_string(),
            pair: pair.to_string(),
            price: Decimal::new(10000, 2), // Пример: 100.00
            liquidity: Decimal::new(1000000, 0),
            timestamp: std::time::SystemTime::now()
                .duration_since(std::time::UNIX_EPOCH)
                .unwrap()
                .as_secs(),
        })
    }
    
    fn execute_swap(
        &self,
        pair: &str,
        amount: Decimal,
        min_output: Decimal,
        simulation: bool,
    ) -> Result<String> {
        if simulation {
            log::info!("СИМУЛЯЦИЯ: Своп {} на Raydium, сумма: {}, мин. выход: {}", 
                pair, amount, min_output);
            return Ok("simulation_tx_signature".to_string());
        }
        
        // ЗАГЛУШКА: реальная реализация свопа через Raydium
        // Необходимо:
        // 1. Построить инструкцию swap
        // 2. Создать транзакцию
        // 3. Подписать и отправить
        
        log::info!("Выполнение свопа {} на Raydium", pair);
        Ok("real_tx_signature".to_string())
    }
}

/// Реализация для Orca
pub struct OrcaDex {
    // Аналогично Raydium
}

impl OrcaDex {
    pub fn new() -> Self {
        Self {}
    }
}

impl DexInterface for OrcaDex {
    fn get_price(&self, pair: &str) -> Result<DexPrice> {
        log::debug!("Получение цены {} на Orca", pair);
        
        Ok(DexPrice {
            dex_name: "orca".to_string(),
            pair: pair.to_string(),
            price: Decimal::new(10050, 2), // Пример: 100.50 (разница для арбитража)
            liquidity: Decimal::new(800000, 0),
            timestamp: std::time::SystemTime::now()
                .duration_since(std::time::UNIX_EPOCH)
                .unwrap()
                .as_secs(),
        })
    }
    
    fn execute_swap(
        &self,
        pair: &str,
        amount: Decimal,
        min_output: Decimal,
        simulation: bool,
    ) -> Result<String> {
        if simulation {
            log::info!("СИМУЛЯЦИЯ: Своп {} на Orca, сумма: {}, мин. выход: {}", 
                pair, amount, min_output);
            return Ok("simulation_tx_signature".to_string());
        }
        
        log::info!("Выполнение свопа {} на Orca", pair);
        Ok("real_tx_signature".to_string())
    }
}

/// Менеджер для работы с несколькими DEX
pub struct DexManager {
    dexes: HashMap<String, Box<dyn DexInterface + Send + Sync>>,
}

impl DexManager {
    pub fn new(enabled_dexes: &[String]) -> Self {
        let mut dexes: HashMap<String, Box<dyn DexInterface + Send + Sync>> = HashMap::new();
        
        for dex_name in enabled_dexes {
            match dex_name.as_str() {
                "raydium" => {
                    dexes.insert("raydium".to_string(), Box::new(RaydiumDex::new()));
                }
                "orca" => {
                    dexes.insert("orca".to_string(), Box::new(OrcaDex::new()));
                }
                _ => {
                    log::warn!("Неизвестный DEX: {}", dex_name);
                }
            }
        }
        
        Self { dexes }
    }
    
    /// Получение цен со всех DEX для указанной пары
    pub fn get_all_prices(&self, pair: &str) -> Vec<DexPrice> {
        let mut prices = Vec::new();
        
        for (name, dex) in &self.dexes {
            match dex.get_price(pair) {
                Ok(price) => prices.push(price),
                Err(e) => log::error!("Ошибка получения цены с {}: {}", name, e),
            }
        }
        
        prices
    }
    
    /// Получение DEX по имени
    pub fn get_dex(&self, name: &str) -> Option<&Box<dyn DexInterface + Send + Sync>> {
        self.dexes.get(name)
    }
}

9️. /opt/arb-bot/src/arbitrage.rs
src/arbitrage.rs содержит основную бизнес-логику поиска арбитражных возможностей и выполнения сделок. Центральные сущности: ArbitrageOpportunity (структура описания возможности) и ArbitrageEngine (движок).
Алгоритм работы в текущей базовой версии:
1.Периодически (через monitoring.check_interval_ms) для каждой пары запрашиваются цены со всех активных DEX через DexManager::get_all_prices(pair).
2.Находятся минимальная и максимальная цена.
3.Вычисляется потенциальная прибыль в процентах. Если прибыль ниже arbitrage.min_profit_percent, возможность игнорируется.
4.Рассчитывается безопасный объём сделки (минимум из max_trade_amount_sol и некоторой доли ликвидности).
5.Если режим симуляции включён (safety.simulation_mode = true), execute_swap DEX’ов не отправляет реальных транзакций — баланс не меняется. Если выключен — будет реальная отправка (когда вы реализуете реальные вызовы).
В код заложены предохранители: min_balance_sol, max_consecutive_failures, transaction_timeout_sec, slippage_tolerance. Это важно, чтобы бот не “утонул” при высокой волатильности/ошибках RPC.
Движок не содержит сетевого кода напрямую — он пользуется DexManager и Wallet. Это повышает стабильность и тестируемость. Тут же интегрирован Monitor — все успехи и ошибки записываются централизованно, можно подключить алерты.
Риски: в реальном мире арбитраж — это гонка: риски фронт-раннинга, задержек RPC, неточности котировок. При переходе в mainnet:
 - используйте быстрый платный RPC;
 - ставьте консервативные slippage_tolerance;
 - учитывайте комиссии и MEV-риски;
 - реализуйте атомарные маршруты, если это возможно (или компенсируйте риски лимитами).


// arbitrage.rs — логика поиска и выполнения арбитражных сделок
// Назначение: анализ цен, расчёт прибыли, выполнение арбитража

use anyhow::Result;
use log::{info, warn, error};
use rust_decimal::Decimal;
use std::sync::Arc;
use tokio::time::{sleep, Duration};

use crate::config::Config;
use crate::wallet::Wallet;
use crate::dex::{DexManager, DexPrice};
use crate::monitor::Monitor;

/// Структура арбитражной возможности
#[derive(Debug)]
struct ArbitrageOpportunity {
    pair: String,
    buy_dex: String,
    sell_dex: String,
    buy_price: Decimal,
    sell_price: Decimal,
    profit_percent: Decimal,
    amount: Decimal,
}

pub struct ArbitrageEngine {
    config: Config,
    wallet: Arc<Wallet>,
    monitor: Arc<Monitor>,
    dex_manager: DexManager,
    consecutive_failures: u32,
}

impl ArbitrageEngine {
    pub fn new(config: Config, wallet: Arc<Wallet>, monitor: Arc<Monitor>) -> Self {
        let dex_manager = DexManager::new(&config.dex.enabled_dexes);
        
        Self {
            config,
            wallet,
            monitor,
            dex_manager,
            consecutive_failures: 0,
        }
    }
    
    /// Основной цикл работы движка
    pub async fn run(&self) -> Result<()> {
        info!("Запуск основного цикла арбитража");
        
        loop {
            // Проверка баланса
            match self.check_balance().await {
                Ok(false) => {
                    error!("Недостаточный баланс, остановка");
                    break;
                }
                Err(e) => {
                    error!("Ошибка проверки баланса: {}", e);
                }
                _ => {}
            }
            
            // Проверка лимита неудачных попыток
            if self.consecutive_failures >= self.config.safety.max_consecutive_failures {
                error!("Достигнут лимит неудачных попыток ({}), остановка", 
                    self.config.safety.max_consecutive_failures);
                break;
            }
            
            // Поиск арбитражных возможностей
            for pair in &self.config.dex.trading_pairs {
                match self.find_arbitrage_opportunity(pair).await {
                    Ok(Some(opportunity)) => {
                        info!("Найдена арбитражная возможность: {:?}", opportunity);
                        
                        // Выполнение арбитража
                        match self.execute_arbitrage(&opportunity).await {
                            Ok(_) => {
                                info!("✅ Арбитраж выполнен успешно!");
                                self.monitor.record_success(&opportunity);
                                // Сброс счётчика неудач
                                // self.consecutive_failures = 0; // Требуется мутабельность
                            }
                            Err(e) => {
                                error!("❌ Ошибка выполнения арбитража: {}", e);
                                self.monitor.record_failure(&opportunity, &e.to_string());
                                // self.consecutive_failures += 1;
                            }
                        }
                    }
                    Ok(None) => {
                        // Нет возможностей для данной пары
                    }
                    Err(e) => {
                        error!("Ошибка поиска арбитража для {}: {}", pair, e);
                    }
                }
            }
            
            // Пауза перед следующей итерацией
            sleep(Duration::from_millis(self.config.monitoring.check_interval_ms)).await;
        }
        
        Ok(())
    }
    
    /// Проверка баланса кошелька
    async fn check_balance(&self) -> Result<bool> {
        let balance = self.wallet.get_balance().await?;
        
        if balance < self.config.safety.min_balance_sol {
            warn!("Баланс {} SOL ниже минимального {} SOL", 
                balance, self.config.safety.min_balance_sol);
            return Ok(false);
        }
        
        Ok(true)
    }
    
    /// Поиск арбитражной возможности для торговой пары
    async fn find_arbitrage_opportunity(&self, pair: &str) -> Result<Option<ArbitrageOpportunity>> {
        // Получение цен со всех DEX
        let prices = self.dex_manager.get_all_prices(pair);
        
        if prices.len() < 2 {
            return Ok(None);
        }
        
        // Поиск минимальной и максимальной цены
        let min_price = prices.iter().min_by_key(|p| p.price).unwrap();
        let max_price = prices.iter().max_by_key(|p| p.price).unwrap();
        
        // Расчёт потенциальной прибыли
        let profit_percent = ((max_price.price - min_price.price) / min_price.price) * Decimal::new(100, 0);
        
        // Проверка минимальной прибыльности
        let min_profit = Decimal::from_f64_retain(self.config.arbitrage.min_profit_percent)
            .unwrap_or(Decimal::ZERO);
        
        if profit_percent < min_profit {
            return Ok(None);
        }
        
        // Расчёт оптимальной суммы сделки (с учётом ликвидности)
        let max_amount = Decimal::from_f64_retain(self.config.arbitrage.max_trade_amount_sol)
            .unwrap_or(Decimal::ONE);
        let amount = max_amount.min(min_price.liquidity / Decimal::new(100, 0));
        
        Ok(Some(ArbitrageOpportunity {
            pair: pair.to_string(),
            buy_dex: min_price.dex_name.clone(),
            sell_dex: max_price.dex_name.clone(),
            buy_price: min_price.price,
            sell_price: max_price.price,
            profit_percent,
            amount,
        }))
    }
    
    /// Выполнение арбитражной сделки
    async fn execute_arbitrage(&self, opportunity: &ArbitrageOpportunity) -> Result<()> {
        info!("Выполнение арбитража: покупка на {} по {}, продажа на {} по {}",
            opportunity.buy_dex, opportunity.buy_price,
            opportunity.sell_dex, opportunity.sell_price);
        
        let simulation = self.config.safety.simulation_mode;
        
        // Шаг 1: Покупка на DEX с низкой ценой
        let buy_dex = self.dex_manager.get_dex(&opportunity.buy_dex)
            .ok_or_else(|| anyhow::anyhow!("DEX {} не найден", opportunity.buy_dex))?;
        
        let min_output_buy = opportunity.amount * opportunity.buy_price 
            * (Decimal::ONE - Decimal::from_f64_retain(self.config.arbitrage.slippage_tolerance / 100.0).unwrap());
        
        let buy_tx = buy_dex.execute_swap(
            &opportunity.pair,
            opportunity.amount,
            min_output_buy,
            simulation,
        )?;
        
        info!("Транзакция покупки: {}", buy_tx);
        
        // Шаг 2: Продажа на DEX с высокой ценой
        let sell_dex = self.dex_manager.get_dex(&opportunity.sell_dex)
            .ok_or_else(|| anyhow::anyhow!("DEX {} не найден", opportunity.sell_dex))?;
        
        let min_output_sell = opportunity.amount * opportunity.sell_price 
            * (Decimal::ONE - Decimal::from_f64_retain(self.config.arbitrage.slippage_tolerance / 100.0).unwrap());
        
        let sell_tx = sell_dex.execute_swap(
            &opportunity.pair,
            opportunity.amount,
            min_output_sell,
            simulation,
        )?;
        
        info!("Транзакция продажи: {}", sell_tx);
        
        Ok(())
    }
}

1️1️.  /opt/arb-bot/src/monitor.rs
src/monitor.rs — слой мониторинга и телеметрии. В базовой версии Monitor предоставляет методы record_success и record_failure, которые логируют события выполнения арбитражных сделок. В реальной эксплуатации сюда подключают:
Telegram-уведомления (через бота и chat_id из .env);
Webhook’и для Slack/Discord;
метрики для Prometheus (через экспортер или pushgateway);
отправку событий в ELK/Graylog.
Цель модуля — централизовать контроль здоровья бота и результативности arbs. Это важно для продакшн-подхода: нужно знать, сколько успешных/неуспешных попыток, какая средняя прибыль, когда последний успех, и есть ли серия неудач.
С точки зрения безопасности, Monitor не должен логировать секреты и приватные ключи. Желательно не печатать сырой текст транзакции, а только её сигнатуру. Для аудита полезно сохранять сводки в локальный файл (указанный в monitoring.log_file) и ротацию логов настроить через logrotate.
Расширение: можно добавить QoS оповещения — например, “если за 10 минут не было ни одной попытки, или 5+ ошибок подряд — прислать алерт”. В связке с systemd Restart=on-failure и внешним мониторингом (например, Healthchecks.io) можно добиться высокой надёжности.
// monitor.rs — мониторинг и логирование работы бота
// Назначение: запись статистики, алерты, метрики производительности

use log::info;
use crate::arbitrage::ArbitrageOpportunity;

pub struct Monitor {
    // Здесь могут быть счётчики, метрики и т.д.
}

impl Monitor {
    pub fn new(_config: &crate::config::Config) -> Self {
        Self {}
    }
    
    /// Запись успешной сделки
    pub fn record_success(&self, opportunity: &ArbitrageOpportunity) {
        info!("📊 Успешная сделка: пара={}, прибыль={}%, сумма={}", 
            opportunity.pair, opportunity.profit_percent, opportunity.amount);
        
        // Здесь можно добавить:
        // - Запись в базу данных
        // - Отправку в Telegram
        // - Отправку метрик в Prometheus/Grafana
        // - Webhook уведомления
    }
    
    /// Запись неудачной попытки
    pub fn record_failure(&self, opportunity: &ArbitrageOpportunity, error: &str) {
        info!("📊 Неудачная попытка: пара={}, ошибка={}", 
            opportunity.pair, error);
    }
}

1️2.  /opt/arb-bot/tests/integration_test.rs
tests/integration_test.rs — набор интеграционных тестов. В Rust папка tests — это сценарии, которые запускаются отдельными бинарями и проверяют поведение пакета через публичный API. Это важно для надёжности: каждый рефактор не должен сломать базовую функциональность.
Тесты включают:
“test_config_loading” — базовый каркас для проверки загрузки конфигурации. На практике вы создадите временный config.toml, подмените переменные окружения и проверите, что Config::load() проходит успешно и валидация работает.
“test_profit_calculation” — проверка точности арифметики для прибыли в процентах. Используется rust_decimal, что исключает двоичные ошибки округления, типичные для f64 при деньгах.
“test_dex_price_fetching” — место для моков RPC; в реальных тестах можно использовать mockito и имитировать ответы RPC провайдера/DEX API.
Запуск: cargo test. Эти тесты помогут быстро ловить регрессии в алгоритмах или конфигурации.
Риски: интеграционные тесты не должны зависеть от реального RPC/Сети — иначе они становятся flaky. Лучше мокать сетевые ответы и тестировать чистую логику. Также не закладывайте в тесты секреты — используйте временные файлы и фикстуры.
// integration_test.rs — интеграционные тесты
// Назначение: проверка корректности работы компонентов

#[cfg(test)]
mod tests {
    use arb_bot::config::Config;
    
    #[test]
    fn test_config_loading() {
        // Тест загрузки конфигурации
        // В реальном тесте нужно создать временный config.toml
        
        // Пример проверки валидации
        assert!(true, "Конфигурация должна загружаться корректно");
    }
    
    #[test]
    fn test_profit_calculation() {
        // Тест расчёта прибыли
        use rust_decimal::Decimal;
        
        let buy_price = Decimal::new(100, 0);
        let sell_price = Decimal::new(105, 0);
        let profit = ((sell_price - buy_price) / buy_price) * Decimal::new(100, 0);
        
        assert_eq!(profit, Decimal::new(5, 0), "Прибыль должна быть 5%");
    }
    
    #[tokio::test]
    async fn test_dex_price_fetching() {
        // Тест получения цен с DEX
        // В реальном тесте используется mock RPC
        
        assert!(true, "Получение цен должно работать");
    }
}

1️3.  /opt/arb-bot/run_demo.sh
run_demo.sh — сценарий запуска бота в демонстрационном режиме. Его задача — обеспечить безопасный, воспроизводимый старт на devnet/симуляции, без риска отправки реальных транзакций.
Скрипт:
проверяет наличие config.toml;
убеждается, что simulation_mode = true;
выводит понятные инструкции и “ожидаемый вывод”, чтобы оператор знал, чего ждать;
устанавливает RUST_LOG=info для наглядных логов;
запускает cargo run --release.
Этот файл полезен для новых операторов и для smoke-тестов после обновления. Он не требует systemd, удобен в сессии SSH. При этом он не меняет конфигурацию — только предупреждает о неправильных настройках (например, если simulation_mode=false, он прекращает запуск, чтобы защитить от случайных транзакций).
Риски: запускать run_demo.sh в продакшене не рекомендуется — лучше использовать systemd unit. В демо режиме важно, чтобы RPC указывал на devnet или чтобы execute_swap имел симуляционную ветку. Иначе операции могут списать реальные средства.
#!/bin/bash
# run_demo.sh — запуск бота в демо-режиме с симуляцией
# Назначение: тестирование без реальных транзакций

set -euo pipefail

cd /opt/arb-bot

echo "=== Запуск демо-режима арбитражного бота ==="

# Проверка наличия конфигурации
if [ ! -f config.toml ]; then
    echo "❌ Файл config.toml не найден!"
    echo "Создайте его из config.example.toml:"
    echo "  cp config.example.toml config.toml"
    exit 1
fi

# Проверка режима симуляции
if ! grep -q "simulation_mode = true" config.toml; then
    echo "⚠️  ВНИМАНИЕ: simulation_mode не установлен в true!"
    echo "Для демо-режима установите simulation_mode = true в config.toml"
    exit 1
fi

echo "✅ Режим симуляции активен"
echo ""
echo "Ожидаемый вывод:"
echo "  - Инициализация компонентов"
echo "  - Проверка баланса"
echo "  - Поиск арбитражных возможностей"
echo "  - СИМУЛЯЦИЯ выполнения сделок (без реальных транзакций)"
echo ""
echo "Для остановки нажмите Ctrl+C"
echo ""

# Установка уровня логирования для демо
export RUST_LOG=info

# Запуск
cargo run --release

1️4. /opt/arb-bot/update.sh
update.sh — безопасный и удобный механизм обновления кода бота. Он автоматизирует остановку сервиса, бэкап конфигов, обновление исходников (через git pull при наличии репозитория), пересборку в --release, восстановление конфигов и запуск сервиса.
Пошагово:
1.systemctl stop arb-bot — чтобы остановить текущий процесс и освободить бинарник/ресурсы.
2.Backup: config.toml.backup, .env.backup — предотвращает потерю настроек.
3.git pull (если .git есть) — обновляет код. Если у вас деплой не через git, этот шаг пропускается и не ломает pipeline.
4.cargo build --release — собирает новый бинарник.
5.Возврат backup-конфигов на место — не потеряете вручную заданные поля.
6.systemctl start arb-bot — перезапуск.
Рекомендации:
 - Храните список версий и ченджлогов.
 - После обновления проверяйте journalctl -u arb-bot -f.
 - Если обновление требует миграций конфигов — включите в скрипт автоматическую проверку схемы (версию конфига) и вывод подсказок.
Безопасность: .env и config.toml не должны уходить в репозиторий; бэкапы создаются локально. Права файлов не меняются.

#!/bin/bash
# update.sh — обновление бота до последней версии
# Назначение: git pull, пересборка, перезапуск

set -euo pipefail

cd /opt/arb-bot

echo "=== Обновление арбитражного бота ==="

# Остановка сервиса
echo "Остановка сервиса..."
sudo systemctl stop arb-bot || true

# Резервное копирование конфигурации
echo "Резервное копирование конфигурации..."
cp config.toml config.toml.backup
cp .env .env.backup 2>/dev/null || true

# Обновление кода (если используется git)
if [ -d .git ]; then
    echo "Обновление из git..."
    git pull
fi

# Пересборка
echo "Пересборка проекта..."
cargo build --release

# Восстановление конфигурации
echo "Восстановление конфигурации..."
cp config.toml.backup config.toml
cp .env.backup .env 2>/dev/null || true

# Запуск сервиса
echo "Запуск сервиса..."
sudo systemctl start arb-bot

echo "✅ Обновление завершено"
echo "Проверьте статус: sudo systemctl status arb-bot"

1️5.  /opt/arb-bot/start.sh
start.sh — удобная обёртка для запуска бота, в первую очередь через systemd. Скрипт:
проверяет наличие config.toml;
если сервис уже активен — показывает статус и не делает лишних действий;
если не активен — вызывает systemctl start arb-bot и выводит подсказки по проверке статуса и логов.
Зачем нужен, если есть systemctl? Он уменьшает риск ошибок и обеспечивает единообразный способ запуска, особенно для операторов без опыта работы с systemd. Удобен в документации и автоматизации.
Риски и рекомендации:
 - Убедитесь, что arb-bot.service корректно настроен (см. ниже).
 - Не используйте start.sh для отладки (там лучше run_demo.sh).
 - Если меняли конфиги, а сервис запущен — лучше выполнить systemctl restart arb-bot.

#!/bin/bash
# start.sh — запуск бота
# Назначение: запуск через systemd или напрямую

set -euo pipefail

cd /opt/arb-bot

echo "=== Запуск арбитражного бота ==="

# Проверка наличия конфигурации
if [ ! -f config.toml ]; then
    echo "❌ Файл config.toml не найден!"
    exit 1
fi

# Попытка запуска через systemd
if systemctl is-active --quiet arb-bot; then
    echo "Бот уже запущен"
    sudo systemctl status arb-bot
else
    echo "Запуск через systemd..."
    sudo systemctl start arb-bot
    echo "✅ Бот запущен"
    echo "Проверьте статус: sudo systemctl status arb-bot"
    echo "Просмотр логов: sudo journalctl -u arb-bot -f"
fi

1️6.  /opt/arb-bot/stop.sh
stop.sh — симметричная команда для корректной остановки бота через systemd. Он проверяет, активен ли сервис, и если да — вызывает systemctl stop arb-bot.
Важно в продакшне: корректная остановка закрывает файловые дескрипторы, освобождает порты и позволяет systemd зафиксировать статус. Использование kill -9 не рекомендуется.
Рекомендации: если вы меняете конфиги или обновляете бинарник, лучше делать остановку через данный скрипт (либо через update.sh, который включает остановку сам).

#!/bin/bash
# stop.sh — остановка бота
# Назначение: корректная остановка через systemd

set -euo pipefail

echo "=== Остановка арбитражного бота ==="

if systemctl is-active --quiet arb-bot; then
    sudo systemctl stop arb-bot
    echo "✅ Бот остановлен"
else
    echo "Бот не запущен"
fi

1️7.  /opt/arb-bot/backup_keys.sh
backup_keys.sh — крайне важный скрипт резервного копирования приватных ключей. Он создает GPG-зашифрованный архив keys_backup_YYYYmmdd_HHMMSS.tar.gz.gpg, содержащий директорию /opt/arb-bot/keys.
Алгоритм:
1.Создаёт /opt/arb-bot/backups (если нет).
2.Архивирует каталог keys в stdout и тут же шифрует симметрично gpg --symmetric --cipher-algo AES256 с выводом в файл.
3.Ставит права chmod 400 на архив.
4.Удаляет бэкапы старше 30 дней.
Для восстановления: gpg -d backup.gpg | tar -xzf - -C /opt/arb-bot.
Риски: забытый пароль шифрования = невосстановимые ключи. Пароль нужно хранить в надёжном менеджере секретов. Также рекомендуется периодически тестировать восстановление на отдельной машине, чтобы убедиться, что процесс работает.
Безопасность:
 - Бэкапы храните на отдельном носителе.
 - Никогда не отправляйте архивы незашифрованными.
 - Ограничьте доступ к /opt/arb-bot/backups правами владельца.

#!/bin/bash
# backup_keys.sh — резервное копирование приватных ключей
# Назначение: создание зашифрованной резервной копии ключей

set -euo pipefail

BACKUP_DIR="/opt/arb-bot/backups"
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/keys_backup_$TIMESTAMP.tar.gz.gpg"

echo "=== Резервное копирование ключей ==="

# Создание директории для бэкапов
mkdir -p "$BACKUP_DIR"

# Проверка наличия ключей
if [ ! -d /opt/arb-bot/keys ]; then
    echo "❌ Директория /opt/arb-bot/keys не найдена!"
    exit 1
fi

echo "Создание архива..."
tar -czf - -C /opt/arb-bot keys | gpg --symmetric --cipher-algo AES256 -o "$BACKUP_FILE"

echo "✅ Резервная копия создана: $BACKUP_FILE"
echo ""
echo "Для восстановления используйте:"
echo "  gpg -d $BACKUP_FILE | tar -xzf - -C /opt/arb-bot"
echo ""
echo "⚠️  ВАЖНО: Сохраните пароль шифрования в надёжном месте!"
echo "⚠️  Храните резервные копии на отдельном носителе!"

# Установка прав доступа
chmod 400 "$BACKUP_FILE"

# Удаление старых бэкапов (старше 30 дней)
find "$BACKUP_DIR" -name "keys_backup_*.tar.gz.gpg" -mtime +30 -delete

echo "Старые бэкапы (>30 дней) удалены"
/etc/systemd/system/arb-bot.service
arb-bot.service — unit-файл systemd, обеспечивающий запуск бота как системной службы. Он:
 - стартует после сети;
 - запускает бинарник /opt/arb-bot/target/release/arb-bot в рабочей директории /opt/arb-bot;
 - использует пользователя ubuntu (рекомендуется отдельный non-root пользователь);
 - подключает .env через EnvironmentFile=/opt/arb-bot/.env;
 - перезапускается при сбоях (Restart=on-failure, RestartSec=10s);
 - включает жёсткие ограничения безопасности: NoNewPrivileges=true, ProtectSystem=strict, ProtectHome=true, ReadWritePaths=/opt/arb-bot/logs /var/log/arb-bot, SystemCallFilter=@system-service, лимиты ресурсов (память/CPU).
Эти меры минимизируют поверхность атаки и случайные побочные эффекты. ProtectSystem=strict делает FS read-only, кроме явно разрешённых путей. Это предотвращает порчу системы при баге.
Риски: если вы меняете пути к логам или бинарнику — обновите ReadWritePaths и ExecStart. После правки всегда делайте systemctl daemon-reload.
Для запуска и автозапуска:
sudo systemctl enable --now arb-bot — добавляет в автозагрузку и запускает.
sudo journalctl -u arb-bot -f — для просмотра логов.
[Unit]
# arb-bot.service — systemd unit для арбитражного бота Solana
# Назначение: автоматический запуск, перезапуск при сбоях, логирование
Description=Solana Arbitrage Bot
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
# Пользователь, от имени которого запускается бот
# ВАЖНО: не используйте root! Создайте отдельного пользователя
User=ubuntu
Group=ubuntu

# Рабочая директория
WorkingDirectory=/opt/arb-bot

# Команда запуска
ExecStart=/opt/arb-bot/target/release/arb-bot

# Переменные окружения
Environment="RUST_LOG=info"
EnvironmentFile=/opt/arb-bot/.env

# Автоматический перезапуск при сбое
Restart=on-failure
RestartSec=10s

# Ограничения безопасности
# Запрет на повышение привилегий
NoNewPrivileges=true

# Ограничение доступа к файловой системе
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/opt/arb-bot/logs /var/log/arb-bot

# Ограничение системных вызовов
SystemCallFilter=@system-service
SystemCallErrorNumber=EPERM

# Ограничение ресурсов
LimitNOFILE=65536
MemoryMax=2G
CPUQuota=200%

# Логирование
StandardOutput=journal
StandardError=journal
SyslogIdentifier=arb-bot

[Install]
WantedBy=multi-user.target

18   /etc/logrotate.d/arb-bot
/etc/logrotate.d/arb-bot — конфигурация logrotate, автоматически ротирующая файлы логов /var/log/arb-bot/*.log. Она:
 - выполняет ротацию ежедневно (daily);
 - хранит 30 архивов;
 - сжимает старые логи (compress, delaycompress);
 - пропускает пустые/отсутствующие файлы (notifempty, missingok);
 - создаёт новые логи с правами 0640 ubuntu ubuntu;
 - после ротации пытается systemctl reload arb-bot (чтобы процесс переоткрыл файловый дескриптор логов; если не реализовано явное reopen логов, reload просто безопасно проигнорируется).
Почему это важно: логи растут быстро. Без ротации они могут занять весь диск и привести к отказу сервиса. Ротация обеспечивает предсказуемый объём хранения и долговременный доступ к истории.
Рекомендации:
 - проверяйте, что monitoring.log_file указывает на /var/log/arb-bot/arb-bot.log;
 - убедитесь, что пользователь сервиса (User=ubuntu) имеет доступ на запись в каталог логов;
 - при необходимости настройте отправку архивов в централизованное хранилище (S3/ELK).
# Конфигурация ротации логов для арбитражного бота
# Назначение: автоматическая ротация и сжатие логов для экономии места

/var/log/arb-bot/*.log {
    # Ротация ежедневно
    daily
    
    # Хранить логи за последние 30 дней
    rotate 30
    
    # Сжимать старые логи
    compress
    delaycompress
    
    # Не выдавать ошибку, если лог-файл отсутствует
    missingok
    
    # Не ротировать пустые файлы
    notifempty
    
    # Создавать новый лог-файл с указанными правами
    create 0640 ubuntu ubuntu
    
    # Действия после ротации
    postrotate
        # Перезагрузка сервиса для открытия нового лог-файла
        systemctl reload arb-bot > /dev/null 2>&1 || true
    endscript
}

19  /var/log/arb-bot/example.log
example.log — эталонный пример лога, показывающий, как должен выглядеть корректный запуск и выполнение симуляционной сделки. В нём видны:
 - инициализация (загрузка конфигурации, режим симуляции);
 - публичный ключ кошелька и баланс;
 - последовательность ценовых запросов к DEX (debug-информация);
 - обнаружение арбитражной возможности;
 - выполнение симулированных свопов и фиксация успеха;
 - корректное завершение по сигналу.
Зачем он нужен: как шаблон для операторов и как ориентир при отладке. Если ваши логи отличаются принципиально (например, отсутствуют блоки про “СИМУЛЯЦИЯ” при simulation_mode=true, или вы видите постоянные ошибки RPC), это сигнал проверить конфиг, сеть, зависимости или права.
Рекомендации:
 - настройте tail -f /var/log/arb-bot/arb-bot.log во время отладки;
 - убедитесь, что logrotate не удаляет активный лог раньше времени;
 - в продакшне предпочтительно использовать journald и централизованный сбор, но локальный файл полезен для “быстрых” проверок и интеграции с legacy системами.
2024-01-15 10:30:45 [INFO] === Запуск арбитражного бота Solana ===
2024-01-15 10:30:45 [INFO] Конфигурация загружена успешно
2024-01-15 10:30:45 [INFO] ⚠️  РЕЖИМ СИМУЛЯЦИИ АКТИВЕН — реальные транзакции не выполняются
2024-01-15 10:30:46 [INFO] Кошелёк инициализирован: 7xJ9...k3Qp
2024-01-15 10:30:46 [INFO] Текущий баланс: 5.234 SOL
2024-01-15 10:30:46 [INFO] Все компоненты инициализированы, запуск основного цикла...
2024-01-15 10:30:46 [INFO] Запуск основного цикла арбитража
2024-01-15 10:30:47 [DEBUG] Получение цены SOL/USDC на Raydium
2024-01-15 10:30:47 [DEBUG] Получение цены SOL/USDC на Orca
2024-01-15 10:30:47 [INFO] Найдена арбитражная возможность: ArbitrageOpportunity { pair: "SOL/USDC", buy_dex: "raydium", sell_dex: "orca", buy_price: 100.00, sell_price: 100.50, profit_percent: 0.5, amount: 1.0 }
2024-01-15 10:30:47 [INFO] Выполнение арбитража: покупка на raydium по 100.00, продажа на orca по 100.50
2024-01-15 10:30:47 [INFO] СИМУЛЯЦИЯ: Своп SOL/USDC на Raydium, сумма: 1.0, мин. выход: 99.00
2024-01-15 10:30:47 [INFO] Транзакция покупки: simulation_tx_signature
2024-01-15 10:30:47 [INFO] СИМУЛЯЦИЯ: Своп SOL/USDC на Orca, сумма: 1.0, мин. выход: 99.50
2024-01-15 10:30:47 [INFO] Транзакция продажи: simulation_tx_signature
2024-01-15 10:30:47 [INFO] ✅ Арбитраж выполнен успешно!
2024-01-15 10:30:47 [INFO] 📊 Успешная сделка: пара=SOL/USDC, прибыль=0.5%, сумма=1.0
2024-01-15 10:30:48 [DEBUG] Получение цены SOL/USDT на Raydium
2024-01-15 10:30:48 [DEBUG] Получение цены SOL/USDT на Orca
2024-01-15 10:31:49 [INFO] Получен сигнал завершения (Ctrl+C)
2024-01-15 10:31:49 [INFO] === Завершение работы бота ===



